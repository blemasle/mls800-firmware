language: c
env:
  global:
    - ARDUINO_IDE_VERSION="1.8.5"
before_install:
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_1.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :1 -ac -screen 0 1280x1024x16"
  - sleep 3
  - export DISPLAY=:1.0
  - wget http://downloads.arduino.cc/arduino-$ARDUINO_IDE_VERSION-linux64.tar.xz
  - tar xf arduino-$ARDUINO_IDE_VERSION-linux64.tar.xz
  - sudo mv arduino-$ARDUINO_IDE_VERSION /usr/local/share/arduino
  - sudo ln -s /usr/local/share/arduino/arduino /usr/local/bin/arduino
install:
 - arduino --install-library "AS1115"
 - arduino --install-library "E24"
 - arduino --install-library "MCP23017"
 - arduino --install-library "MIDI Library"
 - arduino --install-library "MIDIUSB"
script:
 - arduino --verify --board arduino:avr:micro $PWD/src/MLS800.ino
notifications:
  email:
    on_success: change
    on_failure: change